# Hosted mode scaffold

This folder is reserved for hosted-mode specific runtime/build dependencies and packaging config.

Current status:
- `viz` root package builds hosted browser assets via `npm run build:hosted`
- no hosted-only runtime package is defined yet

Use this folder to keep hosted/server-specific dependencies isolated from browser and desktop workflows.
